-- PostgreSQL init script
-- gen_random_uuid() is built-in since PostgreSQL 14, no extensions needed

-- Database is already created by POSTGRES_DB env variable
-- This script handles any additional initialization

SELECT 'database initialized' AS status;
