import { PRPreviewPageContent } from "@/components/pr/pr-preview-page-content";

export default function PRPreviewLayout({ children }: { children: React.ReactNode }) {
  return <PRPreviewPageContent />;
}
