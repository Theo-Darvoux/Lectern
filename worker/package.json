{
  "name": "lectern-zip-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:node": "vitest run -c vitest.node.config.ts",
    "selfhost": "tsx src/node/server.ts",
    "typecheck:node": "tsc -p tsconfig.node.json"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "client-zip": "^2.4.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.3",
    "@cloudflare/workers-types": "^5.20260808.1",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.0"
  }
}
